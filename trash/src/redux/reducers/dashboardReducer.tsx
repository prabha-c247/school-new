import { Plan } from "../../helper/Types";

const initialPlanState:Plan[] = [];
    // plan state properties

  
  const PlanReducer = (state = initialPlanState, action:any) => {
    // Reducer logic for plan-related actions
    return state;
  };

export default PlanReducer;